document.addEventListener('DOMContentLoaded',()=>{const el=document.getElementById('bookingList');if(!el)return;const demo=[{kind:'Rental',title:'Honda Activa',date:'20 Sep 2026',meta:'2:00 PM – 7:00 PM',total:400,status:'Confirmed'},{kind:'Ride share',title:'Surat → Ahmedabad',date:'20 Sep 2026',meta:'1 seat · 8:00 AM',total:300,status:'Confirmed'}];const list=[...JSON.parse(localStorage.getItem('vrumyBookings')||'[]'),...demo];const render=status=>{const visible=status==='Upcoming'?list.filter(x=>x.status==='Confirmed'):list.filter(x=>x.status===status);el.innerHTML=visible.length?visible.map(x=>`<article class="booking-row"><div><span class="pill">${x.kind}</span><h3 style="margin:8px 0 4px">${x.title}</h3><span class="card-meta">${x.date} · ${x.meta} · ${x.id||'VRM-4521'}</span></div><div style="text-align:right"><b>₹${x.total}</b><br><span class="status">${x.status}</span></div></article>`).join(''):`<div class="empty">No ${status.toLowerCase()} bookings yet.</div>`};render('Upcoming');document.querySelectorAll('.tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));tab.classList.add('active');render(tab.textContent)}))});
