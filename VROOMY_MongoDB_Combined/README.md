# VROOMY — COVID_26 Hackathon Build

MongoDB-backed vehicle rental + empty-seat ride sharing prototype.

### Start

```bat
cd backend
npm install
npm start
```

Optional demo data:

```bat
npm run seed
```

Open `http://localhost:5000`.

See `README_MONGODB.md` for admin, MongoDB, verification, payment and LAN instructions.
